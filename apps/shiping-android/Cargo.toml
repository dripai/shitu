[package]
name = "shiping-android"
version.workspace = true
edition.workspace = true
description = "ShiPing Android shared state and JNI bridge"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[features]
android-jni-check = []

[dependencies]
jni = "=0.22.4"
