# 拾屏产品设计基线

> 当前阶段：UI 原型已进入实现，录屏引擎待实现
> 产品定位：轻量、离线优先的桌面录屏工具

## 1. 核心目标

拾屏服务于会议演示、教程录制、问题复现和临时分享。核心流程必须保持为：选择目标、确认声音、开始录制、停止并获得本地文件。

产品不要求登录，不依赖云端，不内置复杂剪辑器，不在首版加入直播和内容管理。

## 2. 首版功能范围

| 能力 | 状态 | 产品说明 |
| --- | --- | --- |
| 整屏录制 | 待实现 | 默认入口，直接录制主显示器 |
| 窗口录制 | 待实现 | 开始前选择一个可见窗口 |
| 区域录制 | 待实现 | 框选固定屏幕区域 |
| 开始、暂停、继续、停止 | 待实现 | 所有录制方式使用同一控制流程 |
| 系统声音 | 待实现 | 可独立开关 |
| 麦克风 | 待实现 | 可独立开关，不默认启用 |
| 清晰度 | UI 原型 | 自动、720p、1080p、原始分辨率 |
| 帧率 | UI 原型 | 30 FPS、60 FPS；默认 30 FPS |
| 鼠标指针 | 待实现 | 默认显示，相关细项不占用启动页 |
| 开始倒计时 | 待实现 | 默认 3 秒，不占用启动页 |
| 本地保存 | UI 原型 | 主窗口紧凑显示输出目录 |
| 录制状态与计时 | 待实现 | 开始录制后切换为紧凑控制条并显示时长 |
| 全局快捷键与托盘 | 规划中 | 录屏主流程稳定后接入 |
| 异常恢复 | 规划中 | 尽量保留可恢复的未完成录制 |

“UI 原型”表示界面和交互位置已经实现，但不代表底层录制能力可用。

## 3. 界面结构

主程序只保留一个紧凑的无标题栏圆角工具条窗口，录制前、录制中和暂停状态均在同一布局内切换：

1. 左侧固定显示录制状态和计时器，计时器下方显示当前来源、清晰度、帧率或简短状态。
2. 右侧依次提供来源、清晰度、帧率、系统声、麦克风和开始/停止。
3. 未录制时，来源按钮在全屏、窗口、区域之间切换；清晰度和帧率按钮按点击循环选项。
4. 录制中，来源按钮切换为暂停/继续；来源、清晰度和帧率锁定，声音仍可独立开关。
5. 停止和保存结果继续显示在计时器下方，不打开完成弹窗。
6. 鼠标细项、倒计时、保存目录和固定 MP4 格式不常驻占用工具条空间；保存目录与退出等低频操作放入原生右键菜单。

界面基于 Slint 1.17。已核对原生 `Button`、`Switch`、`ComboBox`、`Palette`、`TouchArea`、`FocusScope`、`ContextMenuArea`、`Menu`、`MenuItem` 和 `MenuSeparator`，并检查了 `shi-ui` 现有的 `StatusBar`。原生 `Button` 的公开结构固定为横向图标与文字，不能表达参考设计所需的“圆形图标 + 下方标签 + 紧凑选中态”；公共 `StatusBar` 的固定高度和状态结构也不能容纳录制状态、计时与参数摘要。因此这里只为六个快捷操作和录制状态区自定义视觉结构，点击、键盘和可访问语义仍由原生 `TouchArea`、`FocusScope` 和 `accessible-*` 属性承担；右键菜单完全使用 Slint 原生菜单组件，未创建自定义弹层、菜单窗口或额外窗口。

## 4. 默认值

- 录制目标：整个屏幕。
- 清晰度：1080p。
- 帧率：30 FPS。
- 系统声音：开启。
- 麦克风：关闭。
- 显示鼠标：开启。
- 突出鼠标点击：关闭。
- 开始倒计时：3 秒。
- 输出格式：首版固定为 MP4；编码实现和设备兼容性尚未确认。

## 5. 明确不进入首版

- 摄像头画中画。
- 直播推流。
- 云端上传和账号体系。
- 多轨时间线、字幕和专业剪辑。
- GIF 录制。
- 定时任务和批量录制。
- 水印、贴纸和实时标注。

这些能力会显著增加界面和录制链路复杂度，只有在基础录制稳定后再单独评估。

## 6. 当前实现边界

当前代码只提供可编译、可交互的单窗口 UI 原型。点击录制、选择目标或目录时只显示“待接入”状态，不会创建伪录制文件，也不会调用未经核验的平台 API。窗口拖动已通过 Windows 的 `ReleaseCapture`、`WM_NCLBUTTONDOWN` 和 `HTCAPTION` 接入；当前只在 Windows 构建中启用。
